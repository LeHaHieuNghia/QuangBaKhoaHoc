import { Box } from "@mui/material";

const Participants = () => {
  return (
    <Box
      sx={{
        minHeight: "200vh", // Chiều cao vẫn giữ để có thể cuộn
        width: "100%",
      }}
    >
      {/* Nội dung của bạn ở đây */}
    </Box>
  );
};

export default Participants;
